name: scrumlab_progress_dialog_example
description: A new Flutter application.

version: 1.0.0

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: any
  scrumlab_progress_dialog:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

